{%- doc -%}
  Echoes the maximum hero image URL width for Fibrenet slideshow slides.

  @example
  {% capture hero_max %}{% render 'fibrenet-hero-image-max' %}{% endcapture %}
{%- enddoc -%}
{%- render 'fibrenet-lcp-image-dimensions', context: 'home', part: 'max' -%}
