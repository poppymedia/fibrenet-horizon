{%- comment -%}
  Reference only — Shopify disallows {% include %} inside rendered snippets (e.g. scripts.liquid).
  Keep the fn_lazy_* assign block in sync in:
  - layout/theme.liquid (body, for search modal + bootstrap)
  - snippets/scripts.liquid (head, for fn_defer_slideshow_search)
{%- endcomment -%}
